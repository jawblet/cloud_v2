const express = require('express');
const router = express.Router();
const houseController = require('../controllers/groupController');

//router.get('/:houseId/:groupId', houseController.getGroup);
//router.get('/posts/:houseId/:groupId', houseController.getGroupPosts);
//router.post('/', houseController.createGroup);

module.exports = router; 